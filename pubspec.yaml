name: ai_carrer_path
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Core dependencies for your UI
  google_fonts: ^6.1.0          # For the modern font style
  percent_indicator: ^4.2.3     # For the circular/linear progress bars
  smooth_page_indicator: ^1.1.0 # For the onboarding dots
  flutter_svg: ^2.0.9           # For vector icons (optional if you use SVGs)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Ensure you create this folder structure in your project root
  # even if you don't have images yet, otherwise the app might warn you.
  assets:
    - assets/images/
    - assets/icons/