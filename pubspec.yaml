name: ai_carrer_path
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # Image Picker for Profile Picture Upload
  image_picker: ^1.0.7

  # UI & Design
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  percent_indicator: ^4.2.3
  smooth_page_indicator: ^1.1.0
  flutter_svg: ^2.0.9

  # Local Storage
  shared_preferences: ^2.2.2

  # HTTP requests
  http: ^1.2.1

  # For deep equality in skills comparison
  collection: ^1.18.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets Section - REQUIRED even if empty
  assets:
    - assets/images/
    - assets/icons/