name: ai_carrer_path
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1

  # UI & Design
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  percent_indicator: ^4.2.3
  smooth_page_indicator: ^1.1.0
  flutter_svg: ^2.0.9

  # Add this line for SharedPreferences
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The 'flutter' key must appear ONLY ONCE
flutter:
  uses-material-design: true

  # Asset registration
  assets:
    - assets/images/
    - assets/icons/